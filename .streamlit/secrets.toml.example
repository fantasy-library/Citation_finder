# Copy this file to secrets.toml and fill in your API keys.
# Never commit secrets.toml (it's in .gitignore).

WOS_API_KEY = "your-web-of-science-api-key"
SCOPUS_API_KEY = "your-scopus-api-key"
SCOPUS_INST_TOKEN = "your-scopus-institution-token"
